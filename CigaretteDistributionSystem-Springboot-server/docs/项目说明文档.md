# 卷烟投放管理系统 - 项目说明文档

## 📋 项目概述

**项目名称**: CigaretteDistributionSystem-Springboot-server  
**版本**: 1.0.0  
**描述**: 卷烟投放管理系统服务器端  
**技术栈**: Spring Boot 2.7.18, Java 8, MyBatis-Plus, MySQL 8.0  
**架构模式**: DDD（领域驱动设计）分层架构

---

## 🏗️ 项目架构

本项目采用 **DDD（领域驱动设计）分层架构**，将系统分为五个主要层次：

```
api (接口层)
  ↓
application (应用层)
  ↓
domain (领域层)
  ↑
infrastructure (基础设施层)
  ↑
shared (共享层)
```

### 依赖方向规则

```
api → application → domain ← infrastructure
      ↓           ↑
    shared ←──────┘
```

**规则说明**：
1. ✅ `api` 只能依赖 `application`
2. ✅ `application` 可以依赖 `domain`、`shared`
3. ✅ `domain` 只能依赖 `shared`，不能依赖其他层
4. ✅ `infrastructure` 可以依赖 `domain`（实现接口）
5. ✅ `shared` 不依赖任何层

---

## 📁 各层详细说明

### 1. API 层（接口层）

**路径**: `org.example.api`  
**职责**: 处理HTTP请求，参数校验，响应格式化  
**依赖**: 只能依赖 `application/service`  
**禁止**: 不能直接依赖 `infrastructure`、`domain/repository`

#### 目录结构

```
api/
└── web/
    └── controller/          # REST控制器
```

#### 文件说明

| 文件 | 功能描述 |
|------|---------|
| `DistributionCalculateController.java` | 分配计算控制器，提供分配计划生成、区域客户统计等接口 |
| `ExcelImportController.java` | Excel导入控制器，处理客户信息、卷烟信息等Excel文件导入 |
| `PredictionQueryController.java` | 预测查询控制器，提供卷烟投放预测数据的查询接口 |

---

### 2. Application 层（应用层）

**路径**: `org.example.application`  
**职责**: 用例编排、事务管理、DTO转换  
**依赖**: 可以依赖 `domain/service`、`domain/repository`、`shared`  
**禁止**: 不能直接依赖 `infrastructure/persistence/mapper`

#### 目录结构

```
application/
├── service/                 # 应用服务
│   ├── impl/               # 服务实现
│   └── query/              # 查询服务
├── orchestrator/           # 编排器（用例编排、策略选择）
│   └── provider/           # 比例提供者
│       └── impl/           # 比例提供者实现
├── facade/                 # 门面（统一入口）
├── converter/              # 数据转换器
└── dto/                    # 应用层DTO
```

#### 2.1 Service（应用服务）

| 文件 | 功能描述 |
|------|---------|
| `DistributionCalculateService.java` | 分配计算服务接口，定义分配计划生成的核心业务逻辑 |
| `DistributionCalculateServiceImpl.java` | 分配计算服务实现，协调领域服务完成分配计算 |
| `ExcelImportService.java` | Excel导入服务接口，定义数据导入业务逻辑 |
| `ExcelImportServiceImpl.java` | Excel导入服务实现，处理Excel文件解析和数据导入 |
| `RegionCustomerStatisticsBuildService.java` | 区域客户统计构建服务接口 |
| `RegionCustomerStatisticsBuildServiceImpl.java` | 区域客户统计构建服务实现，构建区域客户统计数据 |
| `BiWeeklyVisitBoostService.java` | 双周访销上浮服务接口 |
| `BiWeeklyVisitBoostServiceImpl.java` | 双周访销上浮服务实现，处理双周访销上浮业务逻辑 |
| `TagExtractionService.java` | 标签提取服务接口 |
| `TagExtractionServiceImpl.java` | 标签提取服务实现，从客户信息中提取标签 |
| `EncodeService.java` | 编码服务接口 |
| `EncodeServiceImpl.java` | 编码服务实现，处理业务编码规则 |
| `DistributionWriteBackService.java` | 分配回写服务，将分配结果写回数据库 |

##### Query Service（查询服务）

| 文件 | 功能描述 |
|------|---------|
| `PredictionQueryService.java` | 预测查询服务接口 |
| `PredictionQueryServiceImpl.java` | 预测查询服务实现，提供预测数据查询功能 |
| `PartitionPredictionQueryService.java` | 分区预测查询服务接口 |
| `PartitionPredictionQueryServiceImpl.java` | 分区预测查询服务实现，处理分区表的预测数据查询 |

#### 2.2 Orchestrator（编排器）

**职责**: 用例编排、策略选择、算法调度

| 文件 | 功能描述 |
|------|---------|
| `StrategyOrchestrator.java` | 策略编排器，协调多个领域服务完成复杂用例 |
| `DistributionAlgorithmEngine.java` | 算法选择引擎，根据策略选择相应的分配算法 |
| `DistributionAllocationOrchestrator.java` | 分配编排器，编排分配计算流程 |
| `StrategyContext.java` | 策略上下文，存储策略执行所需的上下文信息 |
| `StrategyContextBuilder.java` | 策略上下文构建器，构建策略执行上下文 |
| `StrategyExecutionRequest.java` | 策略执行请求DTO |
| `StrategyExecutionResult.java` | 策略执行结果DTO |
| `RegionCustomerMatrix.java` | 区域客户矩阵，存储区域-客户关系数据 |
| `AllocationCalculationResult.java` | 分配计算结果，封装分配计算结果数据 |

##### Provider（比例提供者）

| 文件 | 功能描述 |
|------|---------|
| `GroupRatioProvider.java` | 分组比例提供者接口 |
| `IntegrityGroupRatioProvider.java` | 诚信分组比例提供者实现 |
| `MarketTypeRatioProvider.java` | 市场类型比例提供者实现 |

#### 2.3 Facade（门面）

| 文件 | 功能描述 |
|------|---------|
| `DistributionStrategyManager.java` | 分配策略管理器，为复杂子系统提供统一入口 |

#### 2.4 Converter（转换器）

| 文件 | 功能描述 |
|------|---------|
| `DistributionDataConverter.java` | 分配数据转换器，处理DTO与领域对象之间的转换 |

#### 2.5 DTO（数据传输对象）

| 文件 | 功能描述 |
|------|---------|
| `GenerateDistributionPlanRequestDto.java` | 生成分配计划请求DTO |
| `GenerateDistributionPlanResponseDto.java` | 生成分配计划响应DTO |
| `DataImportRequestDto.java` | 数据导入请求DTO |
| `BaseCustomerInfoImportRequestDto.java` | 客户信息导入请求DTO |
| `CigaretteImportRequestDto.java` | 卷烟信息导入请求DTO |
| `RegionCustomerStatisticsRequestDto.java` | 区域客户统计请求DTO |
| `RegionCustomerStatisticsResponseDto.java` | 区域客户统计响应DTO |
| `QueryCigaretteDistributionRecordDto.java` | 查询卷烟投放记录DTO |
| `QueryCigaretteDistributionResponseDto.java` | 查询卷烟投放响应DTO |
| `UpdateCigaretteRequestDto.java` | 更新卷烟请求DTO |
| `UpdatePredictionGradesRequestDto.java` | 更新预测档位请求DTO |
| `BatchUpdateFromExpressionsRequestDto.java` | 批量更新表达式请求DTO |
| `DeleteAreasRequestDto.java` | 删除区域请求DTO |
| `DeleteDeliveryAreasResponseDto.java` | 删除投放区域响应DTO |
| `CalRegionCustomerNumRequestDto.java` | 计算区域客户数量请求DTO |
| `QueryRequestDto.java` | 查询请求DTO |
| `TotalActualDeliveryResponseDto.java` | 总实际投放响应DTO |

---

### 3. Domain 层（领域层）

**路径**: `org.example.domain`  
**职责**: 核心业务逻辑、业务规则、领域模型  
**依赖**: 只能依赖 `shared`（工具类、常量）  
**禁止**: 不能依赖 `infrastructure`、`application`、`api`

#### 目录结构

```
domain/
├── model/                  # 领域模型
│   ├── entity/             # 实体（聚合根）
│   ├── valueobject/        # 值对象
│   └── tag/                # 标签相关模型
├── service/                # 领域服务
│   ├── algorithm/          # 算法服务
│   │   └── impl/           # 算法实现
│   ├── rule/               # 规则服务
│   │   └── impl/           # 规则实现
│   └── delivery/           # 投放相关服务
└── repository/             # 仓储接口（面向领域）
```

#### 3.1 Model（领域模型）

##### ValueObject（值对象）

| 文件 | 功能描述 |
|------|---------|
| `DeliveryCombination.java` | 投放组合值对象，封装投放方式组合信息 |
| `DeliveryMethodType.java` | 投放方式类型枚举（A-按档位统一投放、B-按档位扩展投放、C-按价位段自选投放、D-选点投放、E-按需投放） |
| `DeliveryExtensionType.java` | 扩展类型枚举（1-档位+区县、2-档位+市场类型、3-档位+城乡分类代码、4-档位+业态、5-档位+市场部、6-档位+商圈类型、7-档位+信用等级、8-档位+诚信自律小组） |

##### Tag（标签模型）

| 文件 | 功能描述 |
|------|---------|
| `TagFilterRule.java` | 标签过滤规则模型，定义标签过滤规则 |

#### 3.2 Service（领域服务）

##### Algorithm Service（算法服务）

**特点**: 纯业务逻辑，无Spring依赖，可独立测试

| 文件 | 功能描述 |
|------|---------|
| `SingleLevelDistributionService.java` | 单区域分配算法服务接口 |
| `SingleLevelDistributionServiceImpl.java` | 单区域分配算法服务实现，实现单区域分配算法（SINGLE_LEVEL） |
| `ColumnWiseAdjustmentService.java` | 列调整分配算法服务接口 |
| `ColumnWiseAdjustmentServiceImpl.java` | 列调整分配算法服务实现，实现多区域无权重分配算法（COLUMN_WISE） |
| `GroupSplittingDistributionService.java` | 分组拆分分配算法服务接口 |
| `GroupSplittingDistributionServiceImpl.java` | 分组拆分分配算法服务实现，实现多区域带权重分配算法（GROUP_SPLITTING） |

##### Rule Service（规则服务）

**特点**: 纯业务逻辑，无Spring依赖，可独立测试

| 文件 | 功能描述 |
|------|---------|
| `BiWeeklyVisitBoostRule.java` | 双周访销上浮规则接口 |
| `BiWeeklyVisitBoostRuleImpl.java` | 双周访销上浮规则实现，处理双周访销上浮业务规则 |
| `TagFilterRule.java` | 标签过滤规则接口 |
| `TagFilterRuleImpl.java` | 标签过滤规则实现，处理标签过滤业务规则 |
| `EncodingRule.java` | 编码规则接口 |
| `EncodingRuleImpl.java` | 编码规则实现，处理业务编码规则 |

##### Delivery Service（投放服务）

| 文件 | 功能描述 |
|------|---------|
| `DeliveryCombinationParser.java` | 投放组合解析器，解析投放组合字符串 |

#### 3.3 Repository（仓储接口）

**特点**: 定义数据访问接口（面向领域），不涉及具体技术实现

| 文件 | 功能描述 |
|------|---------|
| `BaseCustomerInfoRepository.java` | 客户基础信息仓储接口 |
| `CigaretteDistributionInfoRepository.java` | 卷烟投放信息仓储接口 |
| `CigaretteDistributionPredictionRepository.java` | 卷烟投放预测仓储接口 |
| `CigaretteDistributionPredictionPriceRepository.java` | 卷烟投放预测价格仓储接口 |
| `RegionCustomerStatisticsRepository.java` | 区域客户统计仓储接口 |
| `IntegrityGroupMappingRepository.java` | 诚信分组映射仓储接口 |
| `TemporaryCustomerTableRepository.java` | 临时客户表仓储接口 |

---

### 4. Infrastructure 层（基础设施层）

**路径**: `org.example.infrastructure`  
**职责**: 技术实现、框架集成、外部系统适配  
**依赖**: 可以依赖 `domain`（实现领域接口）  
**禁止**: 不能依赖 `application`、`api`

#### 目录结构

```
infrastructure/
├── persistence/            # 持久化
│   ├── po/                # 持久化对象（PO）
│   └── mapper/            # MyBatis Mapper
├── repository/            # 仓储实现
│   └── impl/              # 仓储实现类
├── algorithm/             # 算法适配器（Spring适配器）
│   └── impl/              # 算法适配器实现
├── config/                # 配置
│   └── encoding/          # 编码配置
└── monitor/               # 监控
```

#### 4.1 Persistence（持久化）

##### PO（持久化对象）

**特点**: 数据库实体（带MyBatis-Plus注解）

| 文件 | 功能描述 |
|------|---------|
| `BaseCustomerInfoPO.java` | 客户基础信息持久化对象 |
| `CigaretteDistributionInfoPO.java` | 卷烟投放信息持久化对象 |
| `CigaretteDistributionPredictionPO.java` | 卷烟投放预测持久化对象 |
| `IntegrityGroupMappingPO.java` | 诚信分组映射持久化对象 |

##### Mapper（MyBatis Mapper）

| 文件 | 功能描述 |
|------|---------|
| `BaseCustomerInfoMapper.java` | 客户基础信息Mapper接口 |
| `BaseCustomerInfoMapper.xml` | 客户基础信息Mapper XML映射文件 |
| `CigaretteDistributionInfoMapper.java` | 卷烟投放信息Mapper接口 |
| `CigaretteDistributionInfoMapper.xml` | 卷烟投放信息Mapper XML映射文件 |
| `CigaretteDistributionPredictionMapper.java` | 卷烟投放预测Mapper接口 |
| `CigaretteDistributionPredictionMapper.xml` | 卷烟投放预测Mapper XML映射文件 |
| `CigaretteDistributionPredictionPriceMapper.java` | 卷烟投放预测价格Mapper接口 |
| `CigaretteDistributionPredictionPriceMapper.xml` | 卷烟投放预测价格Mapper XML映射文件 |
| `RegionCustomerStatisticsMapper.java` | 区域客户统计Mapper接口 |
| `RegionCustomerStatisticsMapper.xml` | 区域客户统计Mapper XML映射文件 |
| `IntegrityGroupMappingMapper.java` | 诚信分组映射Mapper接口 |
| `IntegrityGroupMappingMapper.xml` | 诚信分组映射Mapper XML映射文件 |
| `TemporaryCustomerTableMapper.java` | 临时客户表Mapper接口 |
| `TemporaryCustomerTableMapper.xml` | 临时客户表Mapper XML映射文件 |

#### 4.2 Repository（仓储实现）

**特点**: 实现 `domain/repository` 接口，调用 `infrastructure/persistence/mapper`

| 文件 | 功能描述 |
|------|---------|
| `BaseCustomerInfoRepositoryImpl.java` | 客户基础信息仓储实现 |
| `CigaretteDistributionInfoRepositoryImpl.java` | 卷烟投放信息仓储实现 |
| `CigaretteDistributionPredictionRepositoryImpl.java` | 卷烟投放预测仓储实现 |
| `CigaretteDistributionPredictionPriceRepositoryImpl.java` | 卷烟投放预测价格仓储实现 |
| `RegionCustomerStatisticsRepositoryImpl.java` | 区域客户统计仓储实现 |
| `IntegrityGroupMappingRepositoryImpl.java` | 诚信分组映射仓储实现 |
| `TemporaryCustomerTableRepositoryImpl.java` | 临时客户表仓储实现 |

#### 4.3 Algorithm（算法适配器）

**特点**: Spring适配器，调用 `domain/service`，保持向后兼容

| 文件 | 功能描述 |
|------|---------|
| `SingleLevelDistributionAlgorithm.java` | 单区域分配算法适配器接口 |
| `DefaultSingleLevelDistributionAlgorithm.java` | 单区域分配算法适配器实现 |
| `ColumnWiseAdjustmentAlgorithm.java` | 列调整分配算法适配器接口 |
| `DefaultColumnWiseAdjustmentAlgorithm.java` | 列调整分配算法适配器实现 |
| `GroupSplittingDistributionAlgorithm.java` | 分组拆分分配算法适配器接口 |
| `DefaultGroupSplittingDistributionAlgorithm.java` | 分组拆分分配算法适配器实现 |

#### 4.4 Config（配置）

| 文件 | 功能描述 |
|------|---------|
| `EncodingRuleProperties.java` | 编码规则配置属性类，加载编码规则配置 |
| `EncodingRuleRepository.java` | 编码规则仓储，提供编码规则查询功能 |

#### 4.5 Monitor（监控）

| 文件 | 功能描述 |
|------|---------|
| `TransactionMonitor.java` | 事务监控器，监控数据库事务执行情况 |

---

### 5. Shared 层（共享层）

**路径**: `org.example.shared`  
**职责**: 通用工具、常量、异常、跨层DTO  
**依赖**: 不依赖其他层  
**特点**: 无状态、可复用

#### 目录结构

```
shared/
├── util/                   # 工具类
├── helper/                 # 辅助类
├── constants/              # 常量
├── exception/              # 异常处理
└── dto/                    # 共享DTO
```

#### 5.1 Util（工具类）

| 文件 | 功能描述 |
|------|---------|
| `ApiResponses.java` | API响应工具类，提供统一的API响应格式 |
| `CombinationStrategyAnalyzer.java` | 组合策略分析器，分析投放组合策略 |
| `GradeParser.java` | 档位解析器，解析档位字符串 |
| `KmpMatcher.java` | KMP字符串匹配算法实现 |
| `MapValueExtractor.java` | Map值提取器，从Map中提取值 |
| `OrderCycleMatrixCalculator.java` | 订货周期矩阵计算器，计算订货周期矩阵 |
| `PartitionTableManager.java` | 分区表管理器，管理数据库分区表 |
| `RegionNameBuilder.java` | 区域名称构建器，构建区域名称 |
| `RegionRecordBuilder.java` | 区域记录构建器，构建区域记录 |
| `RequestValidators.java` | 请求验证器，验证请求参数 |
| `UploadValidators.java` | 上传验证器，验证上传文件 |

#### 5.2 Helper（辅助类）

| 文件 | 功能描述 |
|------|---------|
| `BaseCustomerTableManager.java` | 客户基础表管理器，管理客户基础表操作 |
| `CigaretteInfoWriter.java` | 卷烟信息写入器，写入卷烟信息 |
| `ExcelParseHelper.java` | Excel解析辅助类，解析Excel文件 |
| `ImportValidationHelper.java` | 导入验证辅助类，验证导入数据 |
| `IntegrityGroupMappingService.java` | 诚信分组映射服务，处理诚信分组映射 |

#### 5.3 Constants（常量）

| 文件 | 功能描述 |
|------|---------|
| `BusinessConstants.java` | 业务常量，定义业务相关常量 |
| `GradeConstants.java` | 档位常量，定义档位相关常量 |
| `TableConstants.java` | 表常量，定义数据库表名常量 |

#### 5.4 Exception（异常处理）

| 文件 | 功能描述 |
|------|---------|
| `GlobalExceptionHandler.java` | 全局异常处理器，统一处理系统异常 |

#### 5.5 DTO（共享DTO）

| 文件 | 功能描述 |
|------|---------|
| `RegionCustomerRecord.java` | 区域客户记录DTO，跨层使用的区域客户记录 |

---

## 🔧 项目依赖说明

### 核心依赖

#### Spring Boot 相关

| 依赖 | 版本 | 用途 |
|------|------|------|
| `spring-boot-starter-web` | 2.7.18 | Web应用支持，提供RESTful API |
| `spring-boot-starter-data-jpa` | 2.7.18 | JPA数据访问支持 |
| `spring-boot-starter-cache` | 2.7.18 | 缓存支持 |
| `spring-boot-starter-validation` | 2.7.18 | 数据验证支持 |
| `spring-boot-starter-test` | 2.7.18 | 测试支持 |

#### 数据库相关

| 依赖 | 版本 | 用途 |
|------|------|------|
| `mysql-connector-java` | 8.0.33 | MySQL数据库驱动 |
| `mybatis-plus-boot-starter` | 3.5.5 | MyBatis-Plus，简化DAO层操作 |
| `h2` | - | H2内存数据库（测试用） |

#### 工具类库

| 依赖 | 版本 | 用途 |
|------|------|------|
| `lombok` | - | 简化Java代码，自动生成getter/setter等 |
| `poi` | 5.2.4 | Apache POI，处理Excel文件 |
| `poi-ooxml` | 5.2.4 | Apache POI OOXML，处理Excel 2007+格式 |

### 构建工具

| 插件 | 版本 | 用途 |
|------|------|------|
| `spring-boot-maven-plugin` | - | Spring Boot Maven插件，打包可执行jar |
| `jacoco-maven-plugin` | 0.8.8 | JaCoCo代码覆盖率插件 |

---

## 📂 资源文件说明

### 配置文件

| 文件 | 功能描述 |
|------|---------|
| `application.yml` | 主配置文件，包含数据库连接、JPA配置、日志配置等 |
| `config/encoding-rules.yml` | 编码规则配置文件，定义投放方式、扩展类型、区域类型等编码规则 |

### Mapper XML文件

| 文件 | 功能描述 |
|------|---------|
| `BaseCustomerInfoMapper.xml` | 客户基础信息SQL映射 |
| `CigaretteDistributionInfoMapper.xml` | 卷烟投放信息SQL映射 |
| `CigaretteDistributionPredictionMapper.xml` | 卷烟投放预测SQL映射 |
| `CigaretteDistributionPredictionPriceMapper.xml` | 卷烟投放预测价格SQL映射 |
| `IntegrityGroupMappingMapper.xml` | 诚信分组映射SQL映射 |
| `RegionCustomerStatisticsMapper.xml` | 区域客户统计SQL映射 |
| `TemporaryCustomerTableMapper.xml` | 临时客户表SQL映射 |

### SQL脚本

| 文件 | 功能描述 |
|------|---------|
| `scripts/create-partition-tables.sql` | 创建分区表的SQL脚本 |

---

## 🎯 核心业务功能

### 1. 分配算法

系统支持三种分配算法：

1. **单区域分配算法（SINGLE_LEVEL）**
   - 适用于单区域分配场景
   - 实现类：`SingleLevelDistributionServiceImpl`

2. **多区域无权重分配算法（COLUMN_WISE）**
   - 适用于多区域无权重分配场景
   - 实现类：`ColumnWiseAdjustmentServiceImpl`

3. **多区域带权重分配算法（GROUP_SPLITTING）**
   - 适用于多区域带权重分配场景
   - 实现类：`GroupSplittingDistributionServiceImpl`

### 2. 业务规则

- **双周访销上浮规则**: 处理双周访销上浮业务逻辑
- **标签过滤规则**: 根据标签过滤客户
- **编码规则**: 处理业务编码规则（投放方式、扩展类型、区域类型等）

### 3. 数据导入

- **客户信息导入**: 支持Excel格式的客户信息导入
- **卷烟信息导入**: 支持Excel格式的卷烟信息导入

### 4. 数据查询

- **预测数据查询**: 查询卷烟投放预测数据
- **分区预测查询**: 支持分区表的预测数据查询
- **区域客户统计**: 统计区域客户数据

---

## 🔄 数据流转

### 典型请求流程

```
1. Controller (API层)
   ↓ 接收HTTP请求，参数校验
2. Application Service (应用层)
   ↓ 用例编排，事务管理
3. Domain Service (领域层)
   ↓ 业务逻辑处理
4. Repository Interface (领域层)
   ↓ 定义数据访问接口
5. Repository Implementation (基础设施层)
   ↓ 实现数据访问
6. Mapper (基础设施层)
   ↓ 执行SQL
7. Database (MySQL)
```

### 响应流程

```
Database → Mapper → Repository Implementation → Repository Interface 
→ Domain Service → Application Service → Controller → HTTP Response
```

---

## 📝 编码规范

### 包命名

- **接口层**: `api/web`
- **应用层**: `application/service`、`application/orchestrator`、`application/facade`
- **领域层**: `domain/model`、`domain/service`、`domain/repository`
- **基础设施层**: `infrastructure/persistence`、`infrastructure/repository`
- **共享层**: `shared/util`、`shared/helper`、`shared/constants`

### 类命名

- **接口**: `*Service`、`*Repository`
- **实现**: `*ServiceImpl`、`*RepositoryImpl`
- **实体**: `*Entity`（领域模型）、`*PO`（持久化对象）
- **值对象**: `*ValueObject`、`*VO`
- **DTO**: `*Dto`、`*DTO`
- **VO**: `*Vo`、`*VO`（View Object）

---

## 🚀 运行说明

### 环境要求

- JDK 8+
- Maven 3.6+
- MySQL 8.0+

### 配置说明

1. **数据库配置**: 修改 `application.yml` 中的数据库连接信息
2. **编码规则配置**: 修改 `config/encoding-rules.yml` 中的编码规则

### 启动方式

```bash
# 使用Maven启动
mvn spring-boot:run

# 或打包后运行
mvn clean package
java -jar target/CigaretteDistributionSystem-Springboot-server-1.0.0.jar
```

### 端口配置

默认端口: `28080`

---

## 📚 相关文档

- `docs/DDD分层架构完整目录结构.md` - DDD分层架构详细说明
- `docs/DDD分层实施方案.md` - DDD分层实施详细方案
- `algorithm docs/` - 分配算法详细说明文档

---

## 🔍 项目特点

1. **DDD分层架构**: 清晰的层次划分，职责明确
2. **领域驱动设计**: 核心业务逻辑集中在领域层
3. **可测试性**: 领域服务无Spring依赖，易于单元测试
4. **可扩展性**: 通过策略模式支持多种分配算法
5. **可维护性**: 代码结构清晰，易于维护和扩展

---

## 📄 许可证

本项目为内部项目，仅供内部使用。

---

**文档生成时间**: 2025-01-27  
**文档版本**: 1.0.0

